export HYPRCORE_PATH=$HOME/.local/share/hyprcore
export PATH=$HYPRCORE_PATH/bin/:$PATH
# export TERMINAL=alacritty
export TERMINAL=kitty

# if command -v mise &> /dev/null; then
#   eval "$(mise activate bash)"
# fi
